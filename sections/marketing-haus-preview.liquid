{% comment %}
  The AI Creator's Marketing Haus — public marketing/preview page.
  Not gated — this is the page non-purchasers land on (linked from the
  locked state in sections/marketing-haus.liquid) to learn what the tool
  does and buy access.
{% endcomment %}

{{ 'marketing-haus.css' | asset_url | stylesheet_tag }}

<div class="mh-marketing">
  <div class="mh-marketing__hero">
    <p class="mh-marketing__eyebrow">{{ section.settings.eyebrow }}</p>
    <h1 class="mh-marketing__heading">{{ section.settings.heading | replace: 'AI', '<span class="mh-heading-accent">AI</span>' }}</h1>
    <div class="mh-marketing__subheading">{{ section.settings.subheading }}</div>
    {% if section.settings.access_product %}
      <a href="{{ section.settings.access_product.url }}" class="mh-btn mh-btn--copy mh-marketing__cta">
        Get Access{% if section.settings.access_product.price %} — {{ section.settings.access_product.price | money }}{% endif %}
      </a>
    {% endif %}
  </div>

  <div class="mh-marketing__video">
    {% if section.settings.demo_video_url != blank %}
      <div class="mh-marketing__video-embed">
        <iframe
          src="{{ section.settings.demo_video_url }}"
          title="{{ section.settings.heading }} demo"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen
        ></iframe>
      </div>
    {% else %}
      <div class="mh-marketing__video-placeholder">Demo video coming soon</div>
    {% endif %}
  </div>

  <div class="mh-marketing__features">
    {% for block in section.blocks %}
      {% if block.type == 'feature' %}
        <div class="mh-marketing__feature" {{ block.shopify_attributes }}>
          <h3>{{ block.settings.title }}</h3>
          <p>{{ block.settings.description }}</p>
        </div>
      {% endif %}
    {% endfor %}
  </div>

  {% if section.settings.access_product %}
    <div class="mh-marketing__footer-cta">
      <a href="{{ section.settings.access_product.url }}" class="mh-btn mh-btn--copy mh-marketing__cta">
        Get Access{% if section.settings.access_product.price %} — {{ section.settings.access_product.price | money }}{% endif %}
      </a>
    </div>
  {% endif %}
</div>

{% schema %}
{
  "name": "Marketing Haus Preview",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow text",
      "default": "Black Sheep Creations & Inspirations"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "The AI Creator's Marketing Haus"
    },
    {
      "type": "richtext",
      "id": "subheading",
      "label": "Subheading",
      "default": "<p>Create the mockups, posts, ads, emails, and marketing content around your brand. Customized your way.</p>"
    },
    {
      "type": "url",
      "id": "demo_video_url",
      "label": "Demo video embed URL",
      "info": "YouTube/Vimeo embed URL (not the regular watch link — use the \"Embed\" URL)."
    },
    {
      "type": "product",
      "id": "access_product",
      "label": "Access product"
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature",
      "settings": [
        { "type": "text", "id": "title", "label": "Title" },
        { "type": "textarea", "id": "description", "label": "Description" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Marketing Haus Preview",
      "blocks": [
        { "type": "feature", "settings": { "title": "Product Mockup Studio", "description": "Show your design on a person, a table, a shelf — styled with the props and setting around it, not just a bare product shot." } },
        { "type": "feature", "settings": { "title": "Social Media & Ad Copy Studios", "description": "Platform-ready captions, posts, and ad copy that all sound like the same brand." } },
        { "type": "feature", "settings": { "title": "Brand Kit", "description": "Set your colors, fonts, mood, and voice once — every studio picks it up automatically." } },
        { "type": "feature", "settings": { "title": "Save your favorites", "description": "Keep up to 5 saved prompts per studio, ready to copy anytime." } }
      ]
    }
  ]
}
{% endschema %}
